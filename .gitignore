# Environment / Secrets
.env
*.tfvars
!*.tfvars.example

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
.terraform.lock.hcl
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Docker volumes
es-data/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Python
__pycache__/
.pytest_cache/
*.pyc

# Upstream sync (generated artifacts)
.detection-rules-sync
UPSTREAM_CHANGELOG.md

# Brownfield bulk-import artifacts (see IMPLEMENTATION_STRATEGY.md)
# Cache holds raw Kibana API dumps — large, secret-adjacent, and reproducible.
# imports.tf and import.generated.sh ARE committed to the migration MR (they're
# the reviewable artifact) and removed after the first apply.
.import-cache/
